fragment VariantWithProductFragment on ProductVariant {
  ...VariantFragment
  product {
    id
    handle,
    metafields(first: 10) {
      pageInfo {
        hasNextPage
        hasPreviousPage
      }
      edges {
        cursor
        node {
          id
          key
          value
        }
      }
    }
  }
}