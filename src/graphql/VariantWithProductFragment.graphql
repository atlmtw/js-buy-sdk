fragment VariantWithProductFragment on ProductVariant {
  ...VariantFragment
  product {
    id
    handle,
    metafields(first:10,namespace:"product_fields") {
      pageInfo {
        hasNextPage
        hasPreviousPage
      }
      edges {
        cursor
        node {
          id
          key
          value
        }
      }
    }
  }
}