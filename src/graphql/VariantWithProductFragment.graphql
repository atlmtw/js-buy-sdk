fragment VariantWithProductFragment on ProductVariant {
  ...VariantFragment
  product {
    id
    handle,
    metafields(first:10,namespace:"pproduct_fields") {
      pageInfo {
        hasNextPage
        hasPreviousPage
      }
      edges {
        cursor
        node {
          id
          key
          value
        }
      }
    }
  }
}